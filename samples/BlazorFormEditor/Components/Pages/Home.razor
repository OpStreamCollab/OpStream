@page "/"
@using BlazorFormEditor.Components

<PageTitle>Customer Profile</PageTitle>

@* The demo UI lives in the BlazorFormEditor.View RCL so it can be shared with
   OpStream.DemoHost (mounted there at /samples/form). *@
<FormEditorDemo />
